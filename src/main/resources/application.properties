spring.application.name=admin_service

# MySQL DB Config (same as customer_db)
spring.datasource.url=jdbc:mysql://localhost:3306/customer_db
spring.datasource.username=root
spring.datasource.password=#Narasimha123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port
server.port=8083

# JWT Secret
app.jwt.secret=my-very-secure-and-long-secret-key-123

# Customer Service Base URL (for REST calls)
customer.service.url=http://localhost:8081